# JUnit 5 Parallel Execution Configuration

# Enable parallel execution
junit.jupiter.execution.parallel.enabled=true

# Run test classes and methods in parallel
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Use dynamic thread allocation based on available processors
junit.jupiter.execution.parallel.config.strategy=dynamic

# Use 100% of available processors (can be adjusted based on system resources)
junit.jupiter.execution.parallel.config.dynamic.factor=1.0

# Maximum pool size (optional - JUnit will calculate based on factor if not set)
# junit.jupiter.execution.parallel.config.fixed.parallelism=4

# Timeout for parallel execution (optional)
junit.jupiter.execution.timeout.default=5m

# Display names for better reporting
junit.jupiter.displayname.generator.default=org.junit.jupiter.api.DisplayNameGenerator$ReplaceUnderscores